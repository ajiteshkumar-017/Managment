"use client";
import { Book } from "lucide-react";

export default function Test() {
  return (
    <div className="w-screen h-screen flex items-center justify-center">
      <Book size={80} className="text-black" />
    </div>
  );
}